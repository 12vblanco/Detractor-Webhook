// Credentials removed for confidentiality.
//  this project was built for a client.
// To run locally, copy config.example.js to config.js and fill in your own Supabase project details.
// Use ?mock=true in the URL to preview the dashboard with sample data without a live connection.
const CONFIG = {
  SUPABASE_URL: "your-supabase-url",
  SUPABASE_KEY: "your-supabase-anon-key",
};
